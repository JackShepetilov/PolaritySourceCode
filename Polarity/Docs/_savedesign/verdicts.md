=== SKEPTIC 1 ===
I now have everything I need. I've confirmed the critical `InitializeDependency` cycle hazard, the missing API surface, the chat math error, and the serialization correctness issues. Let me write the adversarial review.

---

## Verdict

**Needs-changes (leaning flawed-as-written).** The two-tier / two-slot architecture is sound and the lifecycle reasoning (load-once-in-`Initialize`, debounced async + sync-on-deactivate, atomic tempв†’promote, run-slot-as-resume-signal) is correct and well-grounded. **But the concrete C++ would not compile or run as drafted**, and three load-bearing correctness claims are wrong: (1) the gather/scatter calls a large surface of subsystem methods that **do not exist** and partly assume the wrong owner; (2) the `InitializeDependency(USaveGameSubsystem)` plan creates a **circular dependency** with the save subsystem's own load that the existing `Initialize` bodies will trip; (3) the **CRC-over-`Serialize()` integrity scheme is non-deterministic and self-defeating** as written, and the chat unread math is **off-by-one and miscounts the groups**. The skeleton is salvageable, but the "concrete, correct, compiles" bar is not met.

## Gaps

| Severity | Issue | Fix |
|---|---|---|
| **Critical** | **`ApplyMeta`/`GatherMeta` call non-existent methods.** `StreamSubsystem` has **no** `SetMetaCurrency`, `SetCompletedRuns`, `SetPlayerStreamerName` (only `AddMetaCurrency`, `SpendMetaCurrency`, `SetPlayerStreamerName` вЂ” `StreamSubsystem.h:92-136`). `ApplyMeta` calls `Stream->SetMetaCurrency(...)` and `Stream->SetCompletedRuns(...)` в†’ won't compile. `GatherMeta` calls `Lore->GetConsumedLoreIDs()` and `ApplyMeta` calls `Lore->RestoreConsumedLoreIDs(...)` вЂ” **neither exists** on `ULoreSubsystem` (only `MarkLoreConsumed`, `IsLoreConsumed`, `GetAllLoreIDs`, `GetConsumedCount` вЂ” `LoreSubsystem.h:46-68`). `ConsumedLoreIDs` is `protected`. | These setters/getters are real header edits on the expensive gameplay subsystems, contradicting the "cpp-only if getters suffice" claim in the file manifest. Add `SetMetaCurrency(int64)`/`SetCompletedRuns(int32)` to `StreamSubsystem.h` and `GetConsumedLoreIDs()`/`RestoreConsumedLoreIDs(const TSet<FName>&)` to `LoreSubsystem.h`. **Update the rebuild summary**: Stream/Lore/XP/Run all need header touches, so this is *not* "cpp-only" for any of them. |
| **Critical** | **`GatherRun`/`ApplyRun` call non-existent methods and the wrong owner.** `Run->GetAcquiredUpgrades()` does not exist вЂ” the real accessor is `GetUpgradeLedger()` returning `const TMap<FGameplayTag,int32>&` (`RunSubsystem.h:120`). `GetAcquiredUpgrades()` exists only on `UUpgradeManagerComponent` and returns `TArray<UUpgradeDefinition*>` (`UpgradeManagerComponent.h:77`) вЂ” a different type and owner; trivial to confuse. `Run->RestoreFromSave(...)` and `XP->RestoreProgress(...)` and `XP->GetProgress()` **do not exist** (`XPSubsystem` exposes only `GetCurrentXP()`/`GetCurrentLevel()` and `AddXP()`; `Progress` is `protected`, `XPSubsystem.h:51-55,107`). | Rename to `GetUpgradeLedger()`. Add `URunSubsystem::RestoreFromSave(...)` and `UXPSubsystem::RestoreProgress(int32,int32)` as real header additions. Note `XP` has no setter at all today вЂ” restoring XP can't go through `AddXP` (it re-fires `OnXPGained`/level-up logic via `CheckLevelUp`); a dedicated silent restore is mandatory. |
| **Critical** | **`InitializeDependency(USaveGameSubsystem)` creates a dependency cycle / use-before-load.** The plan has every meta subsystem call `Collection.InitializeDependency(USaveGameSubsystem::StaticClass())`, while `USaveGameSubsystem::Initialize` в†’ `LoadMetaSynchronous` в†’ `ApplyMeta` в†’ `GetGI<UStreamSubsystem>()` forces Stream/Lore to initialize. But `StreamSubsystem::Initialize` already calls `InitializeDependency(URunSubsystem)` and immediately binds delegates / spawns `ChatBroker` (`StreamSubsystem.cpp:27-41`). Mutual `InitializeDependency` between SaveSubsystem and Stream is a **reentrant init**; UE's collection will either assert or hand back a half-initialized subsystem. The scatter-via-public-setter "tolerates late init" claim is the design's own escape hatch вЂ” but then `InitializeDependency(USaveGameSubsystem)` is pointless and should be dropped. | Pick **one** direction. Recommended: SaveSubsystem depends on nobody; it loads the file into `Meta` in `Initialize` and does **not** scatter during its own `Initialize`. Each meta subsystem, at the **end** of its own `Initialize`, calls `InitializeDependency(USaveGameSubsystem)` then pulls its slice (`ApplyMetaTo(this)`). Remove the SaveSubsystemв†’subsystem push from `LoadMetaSynchronous`. |
| **Critical** | **CRC-over-`Serialize()` integrity check is non-deterministic and likely always "passes" or always "fails."** `ComputeChecksum` runs `Obj.Serialize(Ar)` with a *fresh* `FObjectAndNameAsStringProxyArchive` over a `FMemoryWriter`, but `LoadGameFromSlot` does **not** populate fields via that same proxy archive вЂ” it uses the engine's `USaveGame` serializer (full UObject serialize, including `SaveVersion`, `SavedAtUtc`, and `PayloadChecksum` itself). So the bytes you CRC at save time vs. the bytes you CRC at load time are produced by *different* archive configurations and include different members. Worse: at save you zero `PayloadChecksum`, CRC, then store it вЂ” but `ComputeChecksum` serializes the **whole object including the now-nonzero checksum field on the next call**, and `Serialize` here also walks non-`SaveGame` UObject members unless gated. `ArIsSaveGame=true` is set, which helps, but `FDateTime SavedAtUtc` differs every save and `TSet`/`TMap` iteration order is not guaranteed stable across a save/load cycle в†’ CRC mismatch on legitimate loads. The "deterministic, avoids TMap-iteration false-corruption" graft from D is contradicted by the very `TSet<FName>`/`TMap` fields in `UPolarityMetaSave`. | Drop the self-CRC entirely (engine `USaveGame` already prepends version/integrity headers and won't return a truncated object), **or** checksum the **raw byte buffer** from a single explicit `SaveGameToMemory`/`FMemoryWriter` pass that is *also* the buffer you write to disk (CRC the exact on-disk bytes, store CRC in a sidecar `.crc` file or a fixed-offset header you control), and compare against the same raw read on load. Do **not** CRC by re-serializing a live UObject whose `FDateTime` and container order vary. |
| **Major** | **Chat unread math is off-by-one and the group counts in the worked example are wrong.** The CSV (`ChatMessages.csv`) has rows indexed **0..26 = 27 messages**: intro = rows 0вЂ“8 (**9 msgs**), post_tutorial = rows 9вЂ“26 (**18 msgs**). `GetUnreadChatCount` returns `MaxIdx - LastReadMessageIndex` with `LastReadMessageIndex=-1` default в†’ `8-(-1)=9` for intro (correct) but the design's post-tutorial example says "`26-8 = 18`" only because it assumes the player read *exactly* through index 8. If the player never opened intro (`LastRead=-1`) but tutorial unlocks post_tutorial, `MaxIdx=26`, unread `=26-(-1)=27` вЂ” correct total, fine. The real bug: `MarkChatRead` clamps to `GetMaxUnlockedChatRowIndex()` and `GetUnreadChatCount` subtracts an **inclusive** upper-bound index against a high-water *index*, which is correct only because both are inclusive вЂ” but the design narrates "9 intro messages, rows 0..8" as `8-(-1)=9` while simultaneously initializing `LastReadMessageIndex=-1` meaning "nothing read," so a brand-new player with intro auto-unlocked shows a badge of **9** before ever opening chat. That may be intended, but the doc never auto-unlocks `intro` anywhere вЂ” `Initialize` only builds `ChatGroupRanges`; `intro` is never added to `Meta->Chat.UnlockedGroups`, so a fresh save has **0 unlocked groups в†’ badge 0 в†’ first-visit realtime playback never triggers** for intro. | Explicitly unlock `intro` on fresh-save creation (in `LoadMetaSynchronous` when `!Meta` after `CreateSaveGameObject`, or in a first-boot path). Document that `LastReadMessageIndex=-1` + `intro` unlocked = badge 9 by design. Add a unit assertion for the fresh-save badge value. |
| **Major** | **`WriteSlotAtomic` promote logic is inverted / writes to the wrong path, and async branch races.** `UGameplayStatics::SaveGameToSlot(Obj, Tmp, kUserIndex)` writes to `<SaveGames>/Polarity_Meta_tmp.sav`. The lambda then does `FM.Move(*Bak, *Live, ...)` (liveв†’bak, ok) then `FM.Move(*Live, *TmpF, ...)` вЂ” `IFileManager::Move(Dest, Src)` moves **Srcв†’Dest**, so this is `Move(Live, TmpF)` = TmpFв†’Live, which is correct *if* arg order is (Dest,Src). The draft's comment says "promote tempв†’live" but the earlier INTEL #6 snippet wrote `FM.Move(*Live, *Tmp, true, true)` with the same (Dest,Src) convention вЂ” consistent, but the **bak rotation runs even on the very first save when no live file exists**, and on the async path `bSaveInFlight` is set true but **`GetResumeArenaIndex()` and `ResumeRun()` call `LoadGameFromSlot` with no in-flight guard**, violating the stated "single in-flight guard serializes all save/load." A meta async write in flight during a `ResumeRun()` load is exactly the platform hazard the design cites. | Guard load paths with the same `bSaveInFlight` check (or a proper queue). Skip bak rotation when `!FM.FileExists(Live)`. Add an explicit comment pinning `IFileManager::Move(Dest, Src)` arg order; it's the single most error-prone line. |
| **Major** | **`GetResumeArenaIndex()` and `HasRunSave()` re-read the disk on every call, and the UI binds them per-frame.** `GetResumeArenaIndex` does a full `LoadGameFromSlot` just to peek `CurrentArenaIndex`; `HasRunSave` does `DoesSaveGameExist` (disk stat). The desktop "Resume Run" affordance and badges are described as binding to these. If a UMG binds `HasRunSave()`/`GetResumeArenaIndex()` via a Blueprint property binding (per-frame), this is per-frame disk I/O. | Cache run-slot existence + resume arena in memory on `Initialize`/`SaveRun`/`ClearRun`; expose cached getters. Drive UI off `OnDesktopStateChanged` only. |
| **Major** | **`ComputeChecksum`'s `const_cast<USaveGame&>(Obj).Serialize(Ar)` will also serialize `PayloadChecksum`/`SaveVersion`/`SavedAtUtc`,** and `SavedAtUtc = FDateTime::UtcNow()` is set **before** `ComputeChecksum` in `SaveMetaNow` but the *stored* value is from this save while the *recomputed* value on load uses the loaded `SavedAtUtc` вЂ” those match, so that part is ok, but only by luck of ordering. The fragility (any future reorder of the two lines silently breaks every load) is real. | If keeping any checksum, explicitly exclude header fields from the CRC region (CRC only the gameplay payload), and never include a wall-clock timestamp in the CRC'd region. |
| **Minor** | **`FInt32Range(9, FInt32RangeBound::Inclusive(8))` constructor usage is suspect.** `FInt32Range(Lower, Upper)` takes two `FInt32RangeBound`s or a `(min,max)` value pair depending on overload; mixing a bare `int32` lower with an `FInt32RangeBound` upper (`FInt32Range(0, FInt32RangeBound::Inclusive(8))`) relies on an implicit `int32в†’FInt32RangeBound` (inclusive) conversion. `GetUpperBoundValue()` on an exclusive bound returns the wrong index. This compiles but the inclusivity is easy to get wrong and is the basis of the unread count. | Use explicit `FInt32Range::Inclusive(0, 8)` (the named ctor) and assert `GetUpperBoundValue()` equals the literal CSV max row. Or just store `TMap<FName, int32> GroupMaxRowInclusive` вЂ” simpler, no range-bound semantics to mis-handle. |
| **Minor** | **`#include` / type-visibility gaps in the drafts.** `SaveGameSubsystem.cpp` references `AShooterNPC` (in `GatherRun`'s `TPair<TSubclassOf<AShooterNPC>,int32>`) but includes only the subsystem headers; `RunSubsystem.h` does include `ShooterNPC.h` so it transitively resolves вЂ” fragile but ok. `FInt32Range`/`FInt32RangeBound` need `#include "Math/Range.h"` + `"Math/RangeBound.h"` (only `Math/Range.h` is in the header). `FSoftObjectPath`/`TSoftClassPtr` need `#include "UObject/SoftObjectPath.h"`/`"UObject/SoftObjectPtr.h"`. `FAsyncSaveGameToSlotDelegate` is declared in `Kismet/GameplayStatics.h` (included) вЂ” ok. | Add the missing includes. Spot-verify `GENERATED_BODY()` + `POLARITY_API` are present on all UCLASS/USTRUCT (they are in the drafts). |
| **Minor** | **`WipeMeta` and `LoadMetaSynchronous` re-`CreateSaveGameObject` but never re-unlock `intro`,** so "delete my progress" leaves chat with zero unlocked groups (same root cause as the fresh-save bug). | Centralize fresh-save initialization (unlock `intro`, set default name) in one `InitializeFreshMeta()` helper called by both `LoadMetaSynchronous` (no-file branch) and `WipeMeta`. |
| **Minor** | **`ERunState` stored as `uint8` with `static_cast<ERunState>(In.RunState)` is fine, but `ActivatedAntennaCount`/`RunState` restore has no public mutator path either** вЂ” `RegisterAntennaActivated()` increments and fires events; there's no "set to N silently." `RestoreFromSave` must set the raw fields directly (it can, as a member of `URunSubsystem`), so the single-entry-point restore is the right call вЂ” just confirm it sets `ActivatedAntennaCount`, `CurrentArenaIndex`, `RunState`, `Stats`, `AcquiredUpgrades` as the protected fields, not via the event-firing public API. | Implement `RestoreFromSave` to assign the protected members directly and optionally fire `OnAntennaCountChanged`/`OnArenaEntered` once after, so listeners resync without N spurious increments. |

## Confirmed strengths

- **Two physical slots is the correct call** and the corruption-isolation / `DeleteGameInSlot`-as-abandon-run / run-slot-presence-as-resume-signal reasoning is sound and matches the Hades/Dead-Cells precedent. The meta/run firewall as a filesystem fact (not a convention) is the single best decision here.
- **Lifecycle is right:** load meta synchronously in `Initialize` (subsystems init before the first map's widgets, so the desktop first frame is correct вЂ” confirmed against `StreamSubsystem.cpp:23` init pattern); `bMetaLoaded` guard against pre-load auto-save; `FCoreDelegates::ApplicationWillDeactivate/WillTerminate` for the reliable final flush instead of trusting `Deinitialize`. All correct and well-justified.
- **Explicit-typed-fields over a proxy-archive blob for the *meta* layer is the right trade-off** вЂ” the meta schema evolves most (req #4) and must be migratable/inspectable; blobbing it would be the wrong tool. Reserving struct-array `TArray<FAbilityBankEntry>` instead of a raw `TMap<FGameplayTag,int32>` value (to avoid silent reset on retype) is a genuinely good extensibility hardening.
- **`FRunStats::KillsByEnemy` (`TMap<TSubclassOf<AShooterNPC>,int32>`, `RunSubsystem.h:47`) flatten-to-soft-path-string treatment is correct and necessary**, and the "drop unresolved keys, run-scoped so acceptable, logged" posture is the right call.
- **`uint8` is the persisted contract, not `ERunState`; append-only enumerator rule** вЂ” correct, and `ERunState` (`None/Active/Paused/Ended`, `RunSubsystem.h:14-21`) is indeed small and append-friendly.
- **Tutorial flag owned by the save object, fired by the level (not `UTutorialSubsystem`), idempotent** вЂ” correctly decoupled. `CompletedTutorials` is confirmed a non-`UPROPERTY` plain member, so the design correctly refuses to persist it and keeps it session-scoped.
- **`int32 SaveVersion` field + chain migration over `FCustomVersion` GUID** is the right choice for flat reflected-field `USaveGame`s; the "quarantine forward-version, never downgrade" rule is correct.

**Net:** the architecture and lifecycle are publishable; the implementation drafts are a non-compiling first pass. Before this is "concrete and correct," the author must (a) reconcile every gather/scatter call against the real (and largely missing) subsystem API вЂ” which forces header edits on Stream/Lore/XP/Run and invalidates the "mostly cpp-only" rebuild summary, (b) resolve the `InitializeDependency` cycle by removing the SaveSubsystemв†’subsystem push, (c) replace the re-serialize-to-CRC scheme with a CRC over the actual on-disk byte buffer or drop it, and (d) fix fresh-save `intro` unlock so the chat feature works at all on first boot.

=== SKEPTIC 2 ===
Confirmed. `GetAcquiredUpgrades` exists only on `UUpgradeManagerComponent` (returns `TArray<UUpgradeDefinition*>`), **not** on `URunSubsystem`. The design's `GatherRun` calls `Run->GetAcquiredUpgrades()` expecting `TMap<FGameplayTag,int32>` вЂ” that method does not exist on `URunSubsystem`; the actual accessor is `GetUpgradeLedger()`. And `SetMetaCurrency`, `SetCompletedRuns`, `RestoreProgress`, `RestoreConsumedLoreIDs`, `GetConsumedLoreIDs`, `XP->GetProgress`, `RestoreFromSave` вЂ” **zero matches**, all absent. The design's "cpp-only if existing getters suffice" claim for Stream/XP/Lore is false; every scatter path requires new header methods в†’ rebuild.

I have all the ground truth I need. Writing the adversarial review.

---

## Verdict: **needs-changes**

The two-tier (two-slot) spine, the meta/run firewall, the explicit-fields posture, the `TSubclassOf` flattening, the `bMetaLoaded` guard, and the idempotent tutorial path are all **sound and correct**. But the implementation drafts contain at least three bugs that fail at compile or runtime as written, and one of the system's headline safety features (corruption detection via CRC) is **non-functional** because of a serialization-flag error. None of these are fatal to the architecture вЂ” they are all fixable without re-architecting вЂ” but the design as drafted would not compile, and even after fixing the compile errors the checksum would silently protect nothing. It cannot ship in its current form. The "needs-changes" verdict reflects that the spine is right but the concrete code is not yet correct.

## Gaps

| Severity | Issue | Fix |
|---|---|---|
| **critical** | **CRC checksum is computed over the wrong property set and is non-functional.** `ComputeChecksum()` sets `Ar.ArIsSaveGame = true`. UE's `FProperty::ShouldSerializeValue` (`Property.cpp`) skips every property *without* `CPF_SaveGame` when `Ar.IsSaveGame()` is true. **None** of `UPolarityMetaSave`/`UPolarityRunSave`'s fields are `UPROPERTY(SaveGame)` вЂ” they're all plain `UPROPERTY()`. So the checksum archive serializes ~zero bytes and yields a constant CRC regardless of contents. Corruption, truncation, and tamper would never be detected; the entire "Layer 3 validate-on-load" guarantee is fake. | Drop `Ar.ArIsSaveGame = true` in `ComputeChecksum` so it matches the engine's actual save path (`SaveGameToMemory` uses `FObjectAndNameAsStringProxyArchive(MemoryWriter, false)` вЂ” `ArIsSaveGame` **off**). Better: compute the CRC over the exact `TArray<uint8>` returned by `UGameplayStatics::SaveGameToMemory(Obj, Bytes)` (excluding the header), which is the real on-disk payload, rather than re-serializing the live UObject. Also exclude `PayloadChecksum` from the hashed bytes deterministically (zeroing it before `SaveGameToMemory` works only if it's serialized at a stable offset вЂ” re-hashing the live object's `Serialize()` does not guarantee byte-identity with the disk image). |
| **critical** | **`FInt32Range(0, FInt32RangeBound::Inclusive(8))` does not compile.** `Range.h` exposes only `TRange(A, B)` (both `int32`) and `TRange(BoundsType, BoundsType)` (both bounds) вЂ” there is **no mixed `(int32, FInt32RangeBound)` constructor**, and all ctors are `explicit`. `Initialize()` won't build. | Use either `FInt32Range(FInt32RangeBound::Inclusive(0), FInt32RangeBound::Inclusive(8))` (both bounds) or `FInt32Range(0, 9)` (value-pair, which is `[0,9)` right-**exclusive**). Note the value-pair ctor makes `GetUpperBoundValue()` return the *exclusive* upper (9), so `GetMaxUnlockedChatRowIndex()`/unread math would be off by one. Pick the inclusive-bounds form and keep `GetUpperBoundValue()==8`, or switch the whole scheme to half-open ranges and adjust the math. As drafted it is both a compile error and a latent off-by-one. |
| **critical** | **Every scatter/restore accessor the design calls does not exist; the "cpp-only / uses existing getters" claim is false for Stream, XP, Lore, and Run.** Verified absent in `Source/Polarity`: `UStreamSubsystem::SetMetaCurrency`, `SetCompletedRuns` (only `AddMetaCurrency`/`SpendMetaCurrency` exist); `ULoreSubsystem::GetConsumedLoreIDs`, `RestoreConsumedLoreIDs`; `UXPSubsystem::GetProgress`, `RestoreProgress` (only `GetCurrentXP`/`GetCurrentLevel`, no struct getter, **no setter at all**, and `FSkillState Progress` is `protected`); `URunSubsystem::GetAcquiredUpgrades` (the real method is `GetUpgradeLedger()`, `RunSubsystem.h:120`), `GetActivatedAntennaCount` вњ“ exists, `RestoreFromSave` absent. `GatherRun`/`ApplyRun`/`GatherMeta`/`ApplyMeta` as written **will not compile**. | Either add the missing accessors (all are **header** changes в†’ every one of these subsystem headers must change в†’ full rebuild, contradicting the "cpp-only if getters suffice" rows in the file manifest), or befriend the save subsystem. Rename `Run->GetAcquiredUpgrades()` в†’ `GetUpgradeLedger()` in `GatherRun`. Correct the manifest/rebuild-impact summary: Stream/XP/Lore/Run **all** require header touches; nothing here is Live-Coding-only on first landing. |
| **major** | **Rationale is factually wrong: there is no "native tagged-property serialization, no proxy-archive in the hot path."** `SaveGameToMemory` (`GameplayStatics.cpp:2364`) *always* serializes via `FObjectAndNameAsStringProxyArchive`. The design repeatedly justifies choosing explicit fields by claiming they avoid the proxy archive "for the meta layer that will evolve the most" вЂ” but the proxy archive is used unconditionally for any `USaveGame`. The benefit of explicit fields here is *inspectability/versionability*, not avoiding the proxy path. | Strike the "no proxy-archive in the hot path" justification. The explicit-fields choice is still defensible, but on the correct grounds (typed, migratable, debuggable), not on a serialization-mechanism distinction that doesn't exist. |
| **major** | **`SaveGameToSlot(Obj, Tmp,...)` + raw `IFileManager` promotion reaches behind `ISaveGameSystem` and is not portable/atomic as claimed.** `SaveGameToSlot` routes through `IPlatformFeaturesModule::Get().GetSaveGameSystem()->SaveGame(...)` (`GameplayStatics.cpp:2421-2443`), not a guaranteed `Saved/SaveGames/<Slot>.sav` file. The atomic tempв†’promote `IFileManager::Move` logic only works on the **default desktop** save system where slot==`.sav` file; on any platform save system (or if the path mapping changes) the `.sav`/`.bak`/`_tmp` filenames are wrong and `Promote()` silently fails, leaving you with the raw (non-atomic) `SaveGameToSlot` write you were trying to wrap. (Arg order of `Move(Dest, Src)` is, however, **correct** in the draft вЂ” that part is fine.) | For a PC-only title this is acceptable but must be documented as desktop-only; do not present it as a general "atomic write" layer. Cleaner: serialize with `SaveGameToMemory` and do your own atomic `FFileHelper::SaveArrayToFile` to `вЂ¦/SaveGames/<Slot>_tmp.sav` then `IFileManager::Move`, fully owning the file lifecycle instead of half-using `ISaveGameSystem`. |
| **major** | **Quit-on-PC save guarantee is overstated.** The design leans on `ApplicationWillDeactivateDelegate` + `ApplicationWillTerminateDelegate` for the "sync flush on quit." On desktop, `ApplicationWillDeactivate` fires on **focus loss**, not on a clean `UKismetSystemLibrary::QuitGame`/Alt-F4 exit; `ApplicationWillTerminateDelegate` is **`UE_DEPRECATED(5.3)`** (`CoreDelegates.h:349`) вЂ” using it directly trips a deprecation warning (build error under warnings-as-errors). Combined with the design's own admission that `Deinitialize` is unreliable, there is **no reliable PC quit flush path** in the draft. | Use `FCoreDelegates::GetApplicationWillTerminateDelegate()` (the non-deprecated accessor). More importantly, since meta is debounced, route the menu's Quit button through an explicit `SaveMetaNow()` *before* `QuitGame` (the menu is the only real PC exit path вЂ” `ShooterMainMenuUI` quit, per INTEL #3), rather than trusting app-lifecycle delegates that don't fire on desktop quit. |
| **minor** | **`GetResumeArenaIndex()` and `ResumeRun()`/`HasRunSave()` each independently call `LoadGameFromSlot`/`DoesSaveGameExist`, doing redundant disk I/O.** The desktop draws the "Resume Run вЂ” Arena N" affordance by calling `HasRunSave()` (a `DoesSaveGameExist` stat) then `GetResumeArenaIndex()` (a full `LoadGameFromSlot` just to read one int), then `ResumeRun()` loads the slot a **third** time. INTEL #6 explicitly recommended a lightweight metadata header to avoid deserializing the full save for the menu peek. | Load the run slot once, cache the `UPolarityRunSave*` (mirror the `Meta` member), and serve `HasRunSave/GetResumeArenaIndex/ResumeRun` from the cached object. Or store run metadata (arena index, timestamp) in the **meta** slot so the menu never touches the run slot to render the resume card. |
| **minor** | **`MarkChatRead(GetMaxUnlockedChatRowIndex())` clamp interacts with the range-ctor bug to produce an off-by-one on the unread badge.** Unread = `MaxIdx - LastReadMessageIndex` = `8 - (-1)` = 9 for intro, which is correct **only if** the range upper bound reads back as inclusive `8`. With the value-pair ctor (the form that actually compiles without bounds objects) `GetUpperBoundValue()` returns the exclusive bound, breaking every worked example in INTEL #4 by one. | Tie this fix to the `FInt32Range` ctor fix above; assert the chosen convention with a unit test that reproduces the INTEL #4 worked examples (freshв†’9, post-tutorialв†’18). |
| **minor** | **`RequestMetaSave()` debounce timer is bound to `GetGameInstance()->GetWorld()`'s `FTimerManager`.** During `OpenLevel` map travel the world is torn down; a pending 5s debounce timer registered on the old world is destroyed with it, silently dropping a queued meta save across exactly the menuв†’level transition the meta layer cares about. INTEL #3 flagged `FWorldDelegates::OnWorldCleanup` as the travel signal precisely because world-scoped state dies here. | Either flush-if-dirty on `FWorldDelegates::OnWorldCleanup` (as `UTutorialSubsystem` already does, per INTEL #3), or drive the debounce off a non-world timer (e.g. `FTSTicker`) since the subsystem is GameInstance-scoped and outlives worlds. |

## Confirmed strengths

- **Two physical slots (`Polarity_Meta` / `Polarity_Run`) is the correct call and is correctly justified.** The corruption-blast-radius isolation, `DeleteGameInSlot` as atomic "abandon run," and independent versioning all hold up. This matches the Hades/Dead-Cells precedent the runner-up intel cited and is the right answer for requirement #4's run-vs-meta firewall.
- **Explicit typed `UPROPERTY()` fields on the `USaveGame` subclasses genuinely persist** through the real `SaveGameToSlot` path (verified: `SaveGameToMemory` serializes with `ArIsSaveGame` **off**, so plain `UPROPERTY()` fields are included). The choice is correct even though the stated rationale for it is wrong.
- **The `TSubclassOf<AShooterNPC>` в†’ soft-class-path `FString` flattening for `FRunStats::KillsByEnemy` is the right fix** and the drop-unresolved-keys-on-load posture is appropriate for run-scoped stats. Verified the field is `TMap<TSubclassOf<AShooterNPC>,int32>` at `RunSubsystem.h:47`.
- **Owning `bTutorialCompleted` in the save object, set by a level event, and leaving `UTutorialSubsystem::CompletedTutorials` session-scoped** is correct (`CompletedTutorials` is confirmed a non-`UPROPERTY` plain member; it could never be reflected-serialized anyway). The idempotent single-write `NotifyTutorialCompleted()` and the "tutorial completion is independent of `UTutorialSubsystem`" decoupling satisfy requirement #1 cleanly.
- **`bMetaLoaded` guard against auto-saving-before-load** and the synchronous load in `Initialize` (so the desktop's first frame reads correct state, avoiding the raid-icon flicker) are both correct and important; they address the real first-frame-race and the defaults-overwrite-file failure modes.
- **Chat read-state model** (`UnlockedGroups`/`SeenGroups`/`LastReadMessageIndex` high-water mark, group-range membership as content not save state) is well-shaped; the unread arithmetic is correct *given* the range-bound bug is fixed. The CSV structure (intro rows 0-8, post_tutorial 9-26, the two `[FILE]` rows as the app-drop) was verified against `ChatMessages.csv` and matches the design's assumptions.
- **`FAbilityBankEntry` struct-array over a raw `TMap<FGameplayTag,int32>`** for the future ability bank is the right extensibility call (additive-field-safe vs. silent map-value-retype reset), and reserving the req-#4 containers on `UPolarityMetaSave` so they populate cpp-only later is correct.
- **`int32 SaveVersion` + chain migration + forward-version quarantine** is the appropriate versioning mechanism for flat reflected `USaveGame`s (correctly preferred over `FCustomVersion`, which is only needed for hand-rolled `Serialize()` streams). `ApplicationWillDeactivateDelegate` and `AddWeakLambda` were verified to exist with the used signatures.
