// InventoryDragDropOperation.cpp
// Nothing to implement: the operation is pure payload. The file exists so the class has a
// translation unit of its own rather than living inside whichever widget happened to need it first.

#include "Variant_Shooter/UI/InventoryDragDropOperation.h"
